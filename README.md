Calculator.java → has an empty catch block (Bug/Smell).

LoanProcessor.java → contains a TODO comment (Code Smell).

HelloBank.java → hardcoded password (Vulnerability).

pom.xml → minimal Maven project (Java 17, JUnit).